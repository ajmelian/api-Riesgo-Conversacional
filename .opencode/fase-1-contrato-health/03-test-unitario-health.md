# Tarea 1.3 — Test unitario: HealthControllerTest

**Dependencias:** fase-0 (estructura de directorios)

**Descripción:** Escribir test unitario para el controlador Health sin dependencias externas.

**Criterio de aceptación:**
- [ ] Test `HealthControllerTest` en `tests/Unit/Controllers/HealthControllerTest.php`
- [ ] Verifica que el controlador devuelve código 200
- [ ] Verifica que la respuesta contiene `status` con valor `ok`
- [ ] Verifica que la respuesta contiene `timestamp` en formato ISO8601
- [ ] El test falla inicialmente (RED — no hay controlador aún)

**Ficheros implicados:**
- `tests/Unit/Controllers/HealthControllerTest.php`
